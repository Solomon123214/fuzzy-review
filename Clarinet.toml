[project]
name = "fuzzy-review"
authors = ["Anonymous Developer"]
description = "A blockchain-based verifiable yield tracking platform"
telemetry = false
requirements = []
[contracts.fuzzy-yield-tracker]
path = "contracts/fuzzy-yield-tracker.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
